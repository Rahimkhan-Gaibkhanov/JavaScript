
    'use strict';

    // Задача 1

    // Проверьте, что будет результатом следующей операции:

    console.log(0.1 * 0.2);

    let a = 0.1 * 0.2;

    console.log(a.toFixed(2))

    // Задача 2

    // Проверьте, что будет результатом следующей операции:

    console.log(0.3 - 0.1);

    let b = 0.3 - 0.1;

    console.log(b.toFixed(2));

